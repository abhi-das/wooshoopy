// import { USER_ACTION } from '../constants';

const homeReducer = (state = [], action) => {

    switch (action.type) {
        default:
            return state;
    }
}

export default homeReducer;